{
  "name": "worldcup-2018-sweepstake",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase": "^5.1.0",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4",
    "styled-components": "^3.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "test:once": "CI=true react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "assign": "node --experimental-modules assignCountries.mjs",
    "deploy": "yarn build && cd build && surge --domain worldcup2018.surge.sh"
  },
  "devDependencies": {
    "enzyme": "^3.3.0",
    "enzyme-adapter-react-16": "^1.1.1"
  }
}
